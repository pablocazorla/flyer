<div class="mb-5 p-4">
  <h1 class="font-bold text-xl text-center mb-4">Hello, Flyer!</h1>
  <div class="max-w-lg mx-auto text-sm">
    <div class="bg-sky-500/10 p-4 border border-sky-500 rounded-t-lg">
      <div class="mb-5">
        <label for="fecha" class="block">Fecha</label>
        <input
          type="text"
          id="fecha"
          class="border border-gray-500 rounded-md py-1 px-3 bg-white"
          placeholder="Ej: Vie 30 Mayo · 21:30hs"
        />
      </div>
      <div class="mb-5">
        <label for="direccion" class="block">Dirección</label>
        <input
          type="text"
          id="direccion"
          class="w-full border border-gray-500 rounded-md py-1 px-3 bg-white"
          placeholder="Ej: Paso de los Andes 1634 - Godoy Cruz"
        />
      </div>
      <div class="mb-5">
        <label for="url-entrada-web" class="block">URL Entrada Web</label>
        <input
          type="text"
          id="url-entrada-web"
          class="w-full border border-gray-500 rounded-md py-1 px-3 bg-white"
          placeholder="Url de Entrada Web (para el QR)"
        />
      </div>

      <div class="">
        <button
          class="bg-sky-500 hover:bg-sky-700 text-white font-bold py-2 px-7 rounded-lg cursor-pointer"
          id="generar-flyer">Generar Flyer</button
        >
      </div>
    </div>

    <div class="">
      <canvas
        id="canvas-flyer"
        class="bg-white w-full h-full block shadow-[0_2px_6px_0_rgba(0,0,0,0.4)]"
      ></canvas>
    </div>
    <div
      class="bg-sky-500/10 p-4 border border-sky-500 rounded-b-lg text-center"
    >
      <button
        class="bg-sky-500 hover:bg-sky-700 text-white font-bold py-2 px-7 rounded-lg cursor-pointer"
        id="descargar-flyer">Descargar</button
      >
    </div>
  </div>
</div>

<script>
  import Flyer from "../js/flyer";

  const img = new Image();
  img.src = "/flyer-a5.jpg";
  img.onload = () => {
    const flyer = new Flyer("canvas-flyer", img);
  };
</script>
